@model IEnumerable<Project1403.Models.SubContractor>

@{
    ViewBag.Title = "SubContractorList";
}

<h2>SubContractorList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subcontractorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subcontractorPhone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subcontractorType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subcontractorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subcontractorPhone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subcontractorType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.subcontractorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.subcontractorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.subcontractorID })
        </td>
    </tr>
}

</table>
