@model Project1403.Models.Employee

@{
    ViewBag.Title = "clock";
}

<h2 class="w3-center">Clock-In/Out</h2>
<div class="container">
    <div class="jumbotron">

        
        @using (Html.BeginForm())
        {
        <input class="btn-lg btn-success w3-center" type="submit"  name="isClockedIn" value="IN" formaction=@Url.Action("clock") formmethod="post"/>

        <input class="btn-lg btn-danger w3-center" type="submit"  name="isClockedIn" value="OUT" formaction=@Url.Action("clock") formmethod="post"/>
        }


    </div>
</div>

@Html.Raw(ViewBag.status)



<div>
    @Html.ActionLink("Back to sign-in", "Index")
</div>
